<h1>Мои тренировки</h1>
<%= link_to "Новая тренировка", new_workout_path %>

<% @workouts.each do |workout| %>
  <div>
    <h2><%= link_to workout.title, workout %></h2>
    <p><%= workout.description %></p>
  </div>
<% end %>